export const LOGIN_SUBMIT_BUTTON_PROPS = {
  pendingLabel: "Entrando...",
  delayMs: 0,
  minVisibleMs: 420,
  lockUntilUnmount: false,
  style: { marginTop: "0.4rem", padding: "0.75rem 1rem" }
} as const;
